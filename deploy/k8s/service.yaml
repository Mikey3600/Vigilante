apiVersion: v1
kind: Service
metadata:
  name: vigilante
  labels:
    app: vigilante
spec:
  type: ClusterIP
  ports:
  - port: 3000
    targetPort: 3000
    protocol: TCP
    name: http
  - port: 50051
    targetPort: 50051
    protocol: TCP
    name: grpc
  selector:
    app: vigilante
